bytes = float(input("Enter number of bytes: "))

kilo_bytes = bytes / 1024;
mega_bytes = kilo_bytes / 1024;
giga_bytes = mega_bytes / 1024;

print(f"{bytes} bytes = {kilo_bytes} kilobytes, {mega_bytes} megabytes, {giga_bytes} gigabytes")
        

